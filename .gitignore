# Local prototypes and non-app reference material

# Environment and secrets
.env
.env.*
!.env.development.example
!.env.production.example
*.local.env

# Node / build artifacts
node_modules/
docs/working

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/

# Operations runtime state and logs (maintenance, not committed)
operations/state/
operations/Logs/

# Generated per-machine data (tiles, NDJSON, backups). Never synced via git -
# each machine builds its own through the harvesters.
data/