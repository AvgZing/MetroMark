-1. Resolved! Celebrate by completing the other tasks more thoroughly.
-2. Partially resolved, issues remain as follows: The status panel in the sidebar looks godawful on mobile maybe a light mode dark mode issue and also maybe a clipping issue cuz the whole panel isn't visible, and also the floating status panel is STILL totally broken on mobile (it shows Status - white, and cutoff- and FILTERS? what?). The styling/spacing of the status panel still doesn't look good (on desktop it's more minor spacing fixes on mobile it's a big issue)(and also needs a colored line indicator just like the line view and progress panels). 
-3. Speaking of line view, it takes 2 clicks in the middle of the map to close line view and then unselect the line when in reality it should take 1 click to unselect lines. The stop display now also doesn't update when I click on a different line, even though the status and everything else DOES update. In addition, the stops are still loading in the same order as they were before. FOR THIS TASK ITEM, YOU MAY NOT CHANGE THE LOGIC OF THE MAP AND ITS RENDERING, YOU MAY ONLY - YES, ONLY - IMPACT LINE VIEW WITH THIS CHANGE.
-4. Partially resolved, issue remains as follows: The filters background is a weird gray instead of being consistently styled with the rest of the project, and the modes buttons are now broken where they werent before (the dropdown arrow is above the filters/modes/frequencies text, the show all stops button is now in its own line rather than right justified in the header line?). Plus it just looks bad in the line view the way it is right now with the status and the spacing and all.. ugh.
-5. Why didn't you do this that was here as a task last time? --> Celebrate by separating the Line View stop ordering into a separate file so we can play around with it without risking contaminating the other files; and make sure that this separate file is referenced wherever necessary (in other words: make sure that I'm not still falling back to other ways of ordering the line view that aren't included in this line view ordering separate file)
-6. Resolved! Are you reading this text? If you are, good; if not, then question why not. Celebrate by double checking your work on all of the other tasks before marking this as complete.
-7. STILL UNRESOLVED: The floating mobile header and status panels STILL have a strange blur behind them ON MOBILE that overflows their actual borders and causes this blurring effect even above and below and to the sides, that should be trimmed down so it's just behind the actual translucent containers - or maybe just removed entirely with the floating bubbles made opaque background. 